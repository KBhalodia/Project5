<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <LinearLayout
        android:orientation="vertical"
        android:padding="16dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_horizontal">

        <!-- Cart summary -->
        <TextView
            android:id="@+id/textCartSummary"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Cart: 0 items  |  $0.00"
            android:textSize="16sp"
            android:gravity="center_horizontal"
            android:paddingBottom="12dp" />

        <!-- Bread label -->
        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Choose bread:"
            android:textSize="16sp"
            android:paddingTop="8dp" />

        <!-- Bread spinner -->
        <Spinner
            android:id="@+id/spinnerBread"
            android:layout_width="match_parent"
            android:layout_height="wrap_content" />

        <!-- Protein label -->
        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Choose protein:"
            android:textSize="16sp"
            android:paddingTop="12dp" />

        <!-- Protein spinner -->
        <Spinner
            android:id="@+id/spinnerProtein"
            android:layout_width="match_parent"
            android:layout_height="wrap_content" />

        <!-- Add-ons label -->
        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Add-ons:"
            android:textSize="16sp"
            android:paddingTop="12dp" />

        <!-- Add-ons list -->
        <ListView
            android:id="@+id/listViewAddOns"
            android:layout_width="match_parent"
            android:layout_height="200dp"
            android:choiceMode="multipleChoice" />

        <!-- Live sandwich price -->
        <TextView
            android:id="@+id/textSandwichPrice"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Sandwich price: $0.00"
            android:textSize="16sp"
            android:paddingTop="8dp"
            android:paddingBottom="8dp" />

        <!-- Add sandwich button -->
        <Button
            android:id="@+id/btnAddSandwich"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Add Sandwich to Order" />

    </LinearLayout>
</ScrollView>
